<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <!-- 테스트 -->
 <mapper namespace="com.community.post.mapper.PostMapper">
 	<select id="selectPostListTest" resultType="map">
 		SELECT
 			*
 		FROM
 			`post`
 	</select>
 	
 	<!-- 게시물 목록 조회 -->
 	<select id="selectPostList" resultType="com.community.post.domain.Post">
 		SELECT
 			`id`
			,`userId`
			,`subject`
			,`content`
			,`createdAt`
			,`updatedAt`
		FROM
			`post`
 	</select>
 	
 	<!-- 글 번호로 글 조회 -->
 	<select id="selectPostByPostId" parameterType="int" resultType="com.community.post.domain.Post">
 		SELECT
 			`id`
			,`userId`
			,`subject`
			,`content`
			,`createdAt`
			,`updatedAt`
		FROM
			`post`
		WHERE
			`id` = #{postId}
 	</select>
 	
 	
 	<!-- 게시물 생성 -->
 	<insert id="insertPost" useGeneratedKeys="true" keyProperty="id" parameterType="com.community.post.domain.Post">
 		INSERT INTO `post`
 		(
 			`userId`
			,`subject`
			,`content`
			,`createdAt`
			,`updatedAt`
 		)
 		VALUES
 		(
 			#{userId}
 			, #{subject}
 			, #{content}
 			, NOW()
 			, NOW()
 		)
 	</insert>
 	
 	<delete id="PostDelete" parameterType="int">
 		DELETE FROM `post`
 		WHERE
 			`id` = #{postId}
 	</delete>
 	
 </mapper>